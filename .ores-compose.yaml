schema_version: ores.compose.v1
project: zed-mcp-server
allow_lazy_start: false

services:
  zed-mcp-server:
    name: zed-mcp-server
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/zed-mcp-server.rs"]
