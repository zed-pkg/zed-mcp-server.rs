schema_version: ores.compose.v1
project: zed-mcp-server.rs
allow_lazy_start: false

services:
  zed-mcp-server.rs:
    name: zed-mcp-server.rs
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/zed-mcp-server.rs"]
